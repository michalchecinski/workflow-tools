destination:
  server: 'https://kubernetes.default.svc'

apps:
  external_secrets:
    enabled: true
  home_assistant:
    enabled: true
  joplin_server:
    enabled: true
  nfs_provider_synology:
    enabled: true
  nfs_test:
    enabled: false
